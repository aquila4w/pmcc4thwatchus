import type { Metadata } from "next";

export const metadata: Metadata = { title: "Locate a Church" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
